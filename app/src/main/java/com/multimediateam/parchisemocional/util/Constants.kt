package com.multimediateam.parchisemocional.util

import android.graphics.Color

object Color {
        val RED_PARCHIS = Color.rgb(244, 68, 54)
        val BLUE_PARCHIS = Color.rgb(32, 151, 243)
        val YELLOW_PARCHIS = Color.rgb(213, 195, 48)
        val GREEN_PARCHIS = Color.rgb(76, 175, 81)
    }
